# TypeSafe key (console.typesafe.ai). Leave empty to use the deterministic mock judge.
TYPESAFE_AI_API_KEY=
TRIPWIRE_JUDGE=jev            # jev | mock
TRIPWIRE_POLICY=policies/default.yaml
TRIPWIRE_LOG=data/decisions.jsonl

# Proxy only
PORT=8787
UPSTREAM_BASE_URL=https://api.openai.com/v1
# Leave empty to forward the client's own Authorization header.
UPSTREAM_API_KEY=
